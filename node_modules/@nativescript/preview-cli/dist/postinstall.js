var import_env = require("@webcontainer/env");
function postinstall() {
  if ((0, import_env.isWebContainer)()) {
    console.log("skipping... we are in a WebContainer.");
    return;
  }
}
postinstall();
