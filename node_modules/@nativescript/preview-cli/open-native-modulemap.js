// tries to export the OpenNative modulemap we ship in the shell app
// it can fail in case the version of the shell app doesn't have the modulemap
try {
  module.exports = __non_webpack_require__(
    '~/bundle/open-native-modulemap.json'
  );
} catch (err) {
  module.exports = {};
}
